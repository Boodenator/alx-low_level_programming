#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string to evaluate
 *
 * Return: the length of the string
 */
int _strlen(char *s);
{
char *s;
int len;
s =  "My first strlen!"
len = strlen(s);
printf("%d\n", len);
return (0);
}
